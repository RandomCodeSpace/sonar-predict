<?xml version="1.0" encoding="UTF-8"?>
<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.2.0 http://maven.apache.org/xsd/assembly-2.2.0.xsd">
    <id>skill-dir</id>

    <!--
      Exploded skill bundle. Combined with finalName=skill and
      includeBaseDirectory=true (baseDirectory=sonar-predictor), the dir
      format lands at target/skill/sonar-predictor/ — the layout an agent
      copies into ~/.claude/skills/.
    -->
    <formats>
        <format>dir</format>
    </formats>

    <baseDirectory>sonar-predictor</baseDirectory>
    <includeBaseDirectory>true</includeBaseDirectory>

    <fileSets>
        <!-- SKILL.md at the bundle root — the only file loaded into context. -->
        <fileSet>
            <directory>${project.basedir}/src/main/skill</directory>
            <outputDirectory>.</outputDirectory>
            <includes>
                <include>SKILL.md</include>
            </includes>
        </fileSet>

        <!-- Launchers — sonar made executable, sonar.bat for Windows. -->
        <fileSet>
            <directory>${project.basedir}/src/main/scripts</directory>
            <outputDirectory>bin</outputDirectory>
            <includes>
                <include>sonar</include>
            </includes>
            <fileMode>0755</fileMode>
        </fileSet>
        <fileSet>
            <directory>${project.basedir}/src/main/scripts</directory>
            <outputDirectory>bin</outputDirectory>
            <includes>
                <include>sonar.bat</include>
            </includes>
            <fileMode>0644</fileMode>
        </fileSet>

        <!-- The 10 analyzer plugins fetched by maven-dependency-plugin. -->
        <fileSet>
            <directory>${project.build.directory}/plugins</directory>
            <outputDirectory>plugins</outputDirectory>
            <includes>
                <include>*.jar</include>
            </includes>
        </fileSet>
    </fileSets>

    <!-- The cli + daemon shaded fat jars, renamed without their version. -->
    <dependencySets>
        <dependencySet>
            <outputDirectory>lib</outputDirectory>
            <useProjectArtifact>false</useProjectArtifact>
            <useTransitiveDependencies>false</useTransitiveDependencies>
            <outputFileNameMapping>${artifact.artifactId}.jar</outputFileNameMapping>
            <includes>
                <include>io.github.randomcodespace.sonarpredict:sonar-predictor-cli</include>
                <include>io.github.randomcodespace.sonarpredict:sonar-predictor-daemon</include>
            </includes>
        </dependencySet>
    </dependencySets>
</assembly>
